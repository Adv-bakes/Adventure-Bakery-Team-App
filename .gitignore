# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config
.claude

# Supabase CLI local state
supabase/.temp

# Claude Design / external deck drop folder
training-decks/incoming/
# Local QA render output (PDFs + slide PNGs)
training-decks/qa/

# Python
__pycache__/

# Generated documentation PDFs (root-level, produced by scripts/generate-*.py) —
# the generator scripts are tracked, their rendered output is not.
/dynamic_forms_technical_guide.pdf
/document_numbering_guide.pdf
/derived_reports_technical_guide.pdf

# Local reference PDFs kept in the working copy but not part of the repo.
# Listed by name rather than a *.pdf glob — some PDFs are tracked assets
# (public/sqf-*.pdf are served by the app; ROLES_AND_USER_ADMINISTRATION.pdf).
/Hostinger-Hosting-Guide.pdf

# Word/PDF renderings of the SOP and FRM drafts. These are working documents —
# regenerated and renamed constantly, and large binaries that diff as noise.
# Scoped to sop-drafts/ deliberately (not a repo-wide *.pdf glob) for the same
# reason as above. The authored sources next to them — the .md drafts and the
# .json form schemas — stay tracked; only the rendered output is ignored.
sop-drafts/*.docx
sop-drafts/*.pdf
