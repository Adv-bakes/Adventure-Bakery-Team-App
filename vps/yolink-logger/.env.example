# YoLink User Access Credentials (YoLink app → Settings → Account →
# Advanced Settings → User Access Credentials). Re-issuing invalidates the old pair.
YOLINK_UAID=
YOLINK_SECRET_KEY=
# YoLink Home ID (used to build the MQTT topic yl-home/<HOME_ID>/+/report)
YOLINK_HOME_ID=

# Supabase — use the SERVICE ROLE key (server-side only; bypasses RLS for inserts)
SUPABASE_URL=https://zsukaixinoqmggpxxonn.supabase.co
SUPABASE_KEY=
